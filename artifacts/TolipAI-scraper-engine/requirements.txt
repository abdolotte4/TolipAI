# ── TolipAI Scraper Engine — Fargate Spot requirements ──────────────────────────
# Optimised for ARM64 (Graviton3) Fargate tasks.
# Install: pip install -r requirements.txt
#
# Compatibility contract — verified against crawl4ai==0.8.9 (PyPI requires_dist):
#   crawl4ai==0.8.9 requires:
#     lxml~=5.3            — still requires lxml 5.x (<6); do NOT upgrade lxml to 6.x
#     pillow>=10.4         — no upper bound; Pillow 12.x is fine ✅
#     beautifulsoup4~=4.12 — >=4.12,<5; 4.14.x is within range ✅
#     pydantic>=2.10       — ✅ our pin satisfies this
#     playwright>=1.49.0   — ✅ our pin satisfies this
#     playwright-stealth>=2.0.0 — ✅ our pin satisfies this
#     aiohttp>=3.11.11     — ✅ our pin satisfies this
#     httpx>=0.27.2        — ✅ our pin satisfies this
#     psutil>=6.1.1        — do NOT pin psutil < 6.1.1

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.136.3
uvicorn[standard]==0.49.0
uvloop==0.22.1           # faster event loop on Linux/ARM64
httptools==0.8.0

# ── Database ──────────────────────────────────────────────────────────────────
asyncpg==0.31.0
python-dotenv==1.2.2

# ── HTTP clients ──────────────────────────────────────────────────────────────
httpx[http2]>=0.28.1,<1      # crawl4ai requires >=0.27.2; pin <1 for stability
tenacity==9.1.4
aiohttp==3.14.0

# ── Browser automation (pre-installed in image, listed for version pinning) ──
playwright==1.60.0
playwright-stealth>=2.0.0    # maintained library, replaces hand-rolled stealth
crawl4ai==0.8.9

# ── Redis (job store + retry queue + cache) ───────────────────────────────────
redis[hiredis]==8.0.0     # includes hiredis for speed on ARM64

# ── S3 cache (optional — only used when S3_CACHE_BUCKET is set) ──────────────
aioboto3==15.5.0

# ── AI / LLM ──────────────────────────────────────────────────────────────────
openai>=2.0,<3                  # v2 SDK; <3 guards next major break

# ── Data processing ───────────────────────────────────────────────────────────
pydantic==2.13.4
beautifulsoup4==4.14.3
lxml>=5.3.1,<6                  # crawl4ai 0.8.9 requires ~=5.3 (>=5.3,<6); do NOT upgrade to lxml 6.x

# ── PDF + OCR ─────────────────────────────────────────────────────────────────
PyMuPDF==1.27.2.3
pdfplumber==0.11.9
pytesseract==0.3.13
Pillow>=12.2.0                  # crawl4ai 0.8.9 supports Pillow 12.x

# ── Geo / maps ────────────────────────────────────────────────────────────────
geopy==2.4.1
shapely==2.1.2

# ── Structured logging ────────────────────────────────────────────────────────
python-json-logger==4.1.0

# ── Crypto (AES-256-CBC credential decrypt — matches Node.js crypto-util.ts) ──
cryptography==48.0.0

# ── Misc utils ────────────────────────────────────────────────────────────────
cachetools==7.1.4
python-multipart==0.0.31  # CVE-2024-53498 patched in >=0.0.12; 0.0.31 is safe
orjson==3.11.9
psutil==7.2.2              # crawl4ai==0.8.9 requires >=6.1.1
