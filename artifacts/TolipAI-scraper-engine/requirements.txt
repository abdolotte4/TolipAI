# ── TolipAI Scraper Engine — Fargate Spot requirements ──────────────────────────
# Optimised for ARM64 (Graviton3) Fargate tasks.
# Install: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.0
uvloop==0.21.0           # faster event loop on Linux/ARM64
httptools==0.6.4

# ── Database ──────────────────────────────────────────────────────────────────
asyncpg==0.30.0
python-dotenv==1.0.1

# ── HTTP clients ──────────────────────────────────────────────────────────────
httpx[http2]>=0.27,<1          # relaxed — crawl4ai requires >=0.27; pin <1 for stability
tenacity==9.0.0
aiohttp==3.11.18  # CVE-2024-23334, CVE-2024-52304 patched in >=3.10.11; 3.11.18 is safe

# ── Browser automation (pre-installed in image, listed for version pinning) ──
playwright==1.51.0
crawl4ai==0.6.3

# ── Redis (job store + retry queue + cache) ───────────────────────────────────
redis[hiredis]==5.2.1     # includes hiredis for speed on ARM64

# ── S3 cache (optional — only used when S3_CACHE_BUCKET is set) ──────────────
aioboto3==13.4.0

# ── AI / LLM ──────────────────────────────────────────────────────────────────
openai>=1.32,<2                 # relaxed — crawl4ai has its own openai transitive dep; <2 guards against major break

# ── Data processing ───────────────────────────────────────────────────────────
pydantic==2.11.3
beautifulsoup4==4.13.3
lxml==5.3.1

# ── PDF + OCR ─────────────────────────────────────────────────────────────────
PyMuPDF==1.25.5
pdfplumber==0.11.5
pytesseract==0.3.13
Pillow==10.4.0   # CVE-2024-28219 patched in >=10.3.0; 11.2.1 is safe

# ── Geo / maps ────────────────────────────────────────────────────────────────
geopy==2.4.1
shapely==2.1.0

# ── Structured logging ────────────────────────────────────────────────────────
python-json-logger==3.3.0

# ── Crypto (AES-256-CBC credential decrypt — matches Node.js crypto-util.ts) ──
cryptography==44.0.2

# ── Misc utils ────────────────────────────────────────────────────────────────
cachetools==5.5.2
python-multipart==0.0.20  # CVE-2024-53498 patched in >=0.0.12; 0.0.20 is safe
orjson==3.10.16
psutil==6.1.0
