AWSTemplateFormatVersion: "2010-09-09"
Description: >
  Creates the GithubActionsECRPush IAM role used by the Python-Worker deploy
  workflow. Allows GitHub Actions to push ARM64 images to ECR and deploy to
  ECS Fargate using OIDC — no long-lived AWS credentials stored in GitHub.

  PREREQUISITE: The GitHub OIDC identity provider
  (token.actions.githubusercontent.com) must already exist in the account.
  If it does not, create it first:
    aws iam create-open-id-connect-provider \
      --url https://token.actions.githubusercontent.com \
      --client-id-list sts.amazonaws.com \
      --thumbprint-list 6938fd4d98bab03faadb97b34396831e3780aea1

  One-command deploy (role already exists — use --no-fail-on-empty-changeset):
    aws cloudformation deploy \
      --template-file infrastructure/github-actions-iam-role.yaml \
      --stack-name TolipAI-GitHubActions-Role \
      --capabilities CAPABILITY_NAMED_IAM \
      --no-fail-on-empty-changeset \
      --region us-east-1

  The deploy.yml workflow uses:
    role-to-assume: arn:aws:iam::<AWS_ACCOUNT_ID>:role/GithubActionsECRPush
  No extra GitHub Secret needed — only AWS_ACCOUNT_ID (already set).

Parameters:

  GitHubOrg:
    Type: String
    Default: Agawish24
    Description: GitHub organisation or username that owns the repository.

  GitHubRepo:
    Type: String
    Default: Python-Worker
    Description: Repository name (no org prefix). The role trusts any ref in this repo.

  EcrRepositoryName:
    Type: String
    Default: tolipai-scraper
    Description: ECR repository the workflow pushes images to.

  EcsCluster:
    Type: String
    Default: TolipAI-scraper-cluster
    Description: ECS cluster that hosts the scraper service.

  EcsService:
    Type: String
    Default: tolipai-scraper-engine-service-xop
    Description: ECS service name.

  EcsTaskFamily:
    Type: String
    Default: tolipai-scraper-engine
    Description: ECS task definition family name.

  EcsExecutionRoleName:
    Type: String
    Default: TolipAI-scraper-execution-role
    Description: >
      ECS task execution role. The GitHub Actions role needs iam:PassRole on
      this so ECS can pull secrets and images on task startup.

Resources:

  # ── GitHub Actions Deploy Role ───────────────────────────────────────────────
  # Name must match what deploy.yml passes to role-to-assume.
  # The trust policy references the OIDC provider by its well-known ARN
  # (arn:aws:iam::<account>:oidc-provider/token.actions.githubusercontent.com)
  # rather than a CloudFormation Ref, so the provider does not need to be
  # managed by this stack.
  GithubActionsECRPush:
    Type: AWS::IAM::Role
    Properties:
      RoleName: GithubActionsECRPush
      Description: >
        Assumed by GitHub Actions in Agawish24/Python-Worker to build ARM64
        images, push them to ECR, and deploy to ECS Fargate.
      MaxSessionDuration: 3600
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: GitHubActionsOIDC
            Effect: Allow
            Principal:
              Federated: !Sub "arn:aws:iam::${AWS::AccountId}:oidc-provider/token.actions.githubusercontent.com"
            Action: sts:AssumeRoleWithWebIdentity
            Condition:
              StringEquals:
                "token.actions.githubusercontent.com:aud": sts.amazonaws.com
              StringLike:
                "token.actions.githubusercontent.com:sub":
                  !Sub "repo:${GitHubOrg}/${GitHubRepo}:*"

      Policies:

        # ── ECR: authenticate, pull, push ──────────────────────────────────────
        - PolicyName: ECRPushPull
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: ECRGetToken
                Effect: Allow
                Action: ecr:GetAuthorizationToken
                Resource: "*"
              - Sid: ECRImageOperations
                Effect: Allow
                Action:
                  - ecr:BatchCheckLayerAvailability
                  - ecr:GetDownloadUrlForLayer
                  - ecr:BatchGetImage
                  - ecr:PutImage
                  - ecr:InitiateLayerUpload
                  - ecr:UploadLayerPart
                  - ecr:CompleteLayerUpload
                  - ecr:DescribeRepositories
                  - ecr:DescribeImages
                  - ecr:ListImages
                Resource:
                  !Sub "arn:aws:ecr:${AWS::Region}:${AWS::AccountId}:repository/${EcrRepositoryName}"

        # ── ECS: register task def, update service, describe (for rollback) ───
        - PolicyName: ECSDeployAndRollback
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: ECSReadOnly
                Effect: Allow
                Action:
                  - ecs:DescribeServices
                  - ecs:DescribeTaskDefinition
                  - ecs:ListTaskDefinitions
                  - ecs:DescribeClusters
                  - ecs:ListClusters
                Resource: "*"
              - Sid: ECSRegisterTaskDef
                Effect: Allow
                Action:
                  - ecs:RegisterTaskDefinition
                  - ecs:DeregisterTaskDefinition
                Resource: "*"
              - Sid: ECSUpdateService
                Effect: Allow
                Action: ecs:UpdateService
                Resource:
                  - !Sub "arn:aws:ecs:${AWS::Region}:${AWS::AccountId}:cluster/${EcsCluster}"
                  - !Sub "arn:aws:ecs:${AWS::Region}:${AWS::AccountId}:service/${EcsCluster}/${EcsService}"

        # ── IAM: pass the ECS execution role to newly registered task defs ────
        - PolicyName: IAMPassExecutionRole
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: PassECSExecutionRole
                Effect: Allow
                Action: iam:PassRole
                Resource:
                  !Sub "arn:aws:iam::${AWS::AccountId}:role/${EcsExecutionRoleName}"
                Condition:
                  StringEquals:
                    "iam:PassedToService": ecs-tasks.amazonaws.com

        # ── CloudWatch Logs: allow the CI job to read task logs for debugging ─
        - PolicyName: CloudWatchLogsRead
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: LogsRead
                Effect: Allow
                Action:
                  - logs:GetLogEvents
                  - logs:DescribeLogStreams
                  - logs:DescribeLogGroups
                  - logs:FilterLogEvents
                Resource:
                  - !Sub "arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:/ecs/${EcsTaskFamily}*"
                  - !Sub "arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:/ecs/${EcsTaskFamily}*:*"

Outputs:

  RoleArn:
    Description: ARN of the GithubActionsECRPush role.
    Value: !GetAtt GithubActionsECRPush.Arn
    Export:
      Name: TolipAI-GithubActionsECRPushRoleArn

  DeployCommand:
    Description: One-liner to update this stack.
    Value: !Sub >-
      aws cloudformation deploy
      --template-file infrastructure/github-actions-iam-role.yaml
      --stack-name TolipAI-GitHubActions-Role
      --capabilities CAPABILITY_NAMED_IAM
      --no-fail-on-empty-changeset
      --region ${AWS::Region}
