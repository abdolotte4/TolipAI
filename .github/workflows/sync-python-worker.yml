# Disabled — scraper engine sync is no longer managed via GitHub Actions.
on:
  workflow_dispatch:
jobs:
  disabled:
    name: Disabled workflow
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "This workflow is disabled"
