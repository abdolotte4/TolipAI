{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "install": "echo ROOT CHECK && pwd && ls -la && echo PACKAGE CHECK && cat package.json || echo 'no package.json' && pnpm install --no-frozen-lockfile --prod=false",
    "command": "pnpm --filter @workspace/api-server run build:prod"
  },
  "deploy": {
    "startCommand": "node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "healthcheckPath": "/api/healthz",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}


